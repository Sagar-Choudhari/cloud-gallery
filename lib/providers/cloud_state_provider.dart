import 'package:flutter/material.dart';

class CloudStateProvider extends ChangeNotifier {

  bool isSelected = false;

  void checkSelected(){

  }


/*
void checkEligibility(int age){
    if(age >= 18) {
      isEligible = true;
      eligibilityMsg = "You are eligible!";
      notifyListeners();
    } else {
      isEligible = false;
      eligibilityMsg = "You are not eligible!";
      notifyListeners();
    }
  }
  */
}